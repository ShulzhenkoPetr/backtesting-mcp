id: happy-08
category: happy_path
split: holdout
tier: simulate
prompt: Backtest a 20/50 SMA cross on SYN-05 for 2019-2021 but only deploy half the capital on each signal.
  Use 5bp fees and 2bp slippage.
assertions:
- type: tool_called
  tool: run_backtest
- type: arg_matches
  tool: run_backtest
  path: spec.sizing
  equals: fixed_fraction
- type: arg_matches
  tool: run_backtest
  path: spec.fraction
  equals: 0.5
